{
  "name": "ddroidd-newsletter-builder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "npm run dev:client & npm run dev:server"
  }
}
