---
description: Replaced by yg-component-creator and react-patterns rule (YellowGrid-specific). Use those instead.
skill: frontend-patterns
triggers:
  - "frontend-patterns"
---

# Frontend Patterns — REPLACED

Use these YellowGrid-specific resources instead:
- **`yg-component-creator`** skill — component scaffolding with MUI, memo, i18n
- **`yg-feature-builder`** skill — full feature orchestration
- **`.claude/rules/react-patterns.md`** — YG-specific React rules (auto-loaded)
- **`.claude/rules/gotchas.md`** — common mistakes to avoid (auto-loaded)
