---
phase: quick-260704-p2a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/client/src/blocks/new-collegues.ts
  - app/client/src/blocks/initiatives.ts
  - app/client/src/blocks/hiring.ts
  - app/client/src/blocks/want-to-know-more.ts
  - app/client/src/blocks/disclaimer.ts
  - app/client/src/blocks/template.ts
  - app/client/src/App.tsx
  - app/server/scripts/verify-blocks.ts
  - app/server/package.json
autonomous: true
requirements: [BLOCK-01, BLOCK-02]

must_haves:
  truths:
    - "Each of the 5 remaining hand-authored sections exists as a DDROIDD branded block in the editor"
    - "All 7 branded blocks appear in the editor blocks panel under the DDROIDD category"
    - "A 'New from Template' toolbar button seeds the full 7-section newsletter onto the canvas"
    - "The existing empty <mjml><mj-body> scaffold remains the default on an empty canvas (no auto-seed of the full template)"
    - "Every new block and the full template compile through mjml@4.18.0 with zero errors and non-empty HTML"
  artifacts:
    - path: "app/client/src/blocks/new-collegues.ts"
      provides: "New Collegues branded block, inlined defaults, no mj-attributes/include/style"
    - path: "app/client/src/blocks/initiatives.ts"
      provides: "Meet-ups and Initiatives branded block"
    - path: "app/client/src/blocks/hiring.ts"
      provides: "We're Hiring branded block (yellow role list preserved)"
    - path: "app/client/src/blocks/want-to-know-more.ts"
      provides: "Want To Know More branded block"
    - path: "app/client/src/blocks/disclaimer.ts"
      provides: "Confidentiality disclaimer block (explicit #ffffff bg + black italic text)"
    - path: "app/client/src/blocks/template.ts"
      provides: "TEMPLATE_MJML — all 7 block content strings composed into one <mjml><mj-body> document, alias-free"
    - path: "app/server/scripts/verify-blocks.ts"
      provides: "Headless mjml@4.18.0 compile gate for each block + the template"
  key_links:
    - from: "app/client/src/App.tsx"
      to: "the 5 new block modules + template.ts"
      via: "import + editor.Blocks.add + New from Template button"
      pattern: "editor\\.Blocks\\.add\\('ddroidd-"
    - from: "app/server/scripts/verify-blocks.ts"
      to: "app/client/src/blocks/*.ts"
      via: "relative import of block content strings"
      pattern: "\\.content"
---

<objective>
Re-author the 5 hand-authored newsletter sections not yet covered by an editor block
(new-collegues, initiatives, hiring, want-to-know-more, disclaimer) as grapesjs-mjml
branded block definitions, following the existing `hero.ts` / `projects.ts` pattern and
`BLOCK_DEFAULTS`. Add a "start a new newsletter from the full template" path and a
headless compile gate.

Purpose: Complete the branded-block coverage of the latest `src/pages/index.mjml`
newsletter so a non-developer can assemble the full DDROIDD digest from the blocks panel,
or start from the complete template in one click.

Output: 5 new block modules, a `TEMPLATE_MJML` module, App.tsx wiring (block registration
+ "New from Template" button), and a verify script proving every block and the template
compile cleanly through the pinned server `mjml@4.18.0`.
</objective>

<execution_context>
This is a quick-mode plan. Execute the tasks in order; run the verify gate last.
Do NOT run `git commit` / `git push` — project no-commit rule (`.claude/rules/no-commit.md`).
Report changed files + a suggested commit message at the end.
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/rules/grapesjs.md
@.claude/rules/mjml-email-safety.md
@.claude/skills/new-branded-block/SKILL.md

<!-- The pattern to copy (already re-authored, spike-blessed): -->
@app/client/src/blocks/BLOCK_DEFAULTS.ts
@app/client/src/blocks/hero.ts
@app/client/src/blocks/projects.ts
@app/client/src/App.tsx

<!-- Design/content reference ONLY — do NOT import these into the editor (round-trip is lossy, issues #35/#194/#388): -->
@src/pages/index.mjml
@src/components/head.mjml
@src/sections/new-collegues.mjml
@src/sections/initiatives.mjml
@src/sections/hiring.mjml
@src/sections/want-to-know-more.mjml
@src/sections/disclaimer.mjml

<!-- The production compile path the verify script must mirror exactly: -->
@app/server/src/routes/compile.ts

<interfaces>
Existing block module shape (copy this — see hero.ts / projects.ts):

```ts
import { BLOCK_DEFAULTS as D } from './BLOCK_DEFAULTS';

export const <name>Block = {
  id: 'ddroidd-<name>',
  label: 'DDROIDD <Human Name>',
  category: 'DDROIDD',
  content: `<mj-section background-color="${D.backgroundColor}"> ... </mj-section>`, // bare fragment, no <mjml> root
};
```

BLOCK_DEFAULTS (single source of truth for inlined brand values):
  backgroundColor '#0B1624' | fontFamily 'Calibri, Roboto, Lato, Avenir Next, Verdana, Helvetica, Arial, sans-serif'
  fontSize '16px' | lineHeight '24px' | textColor '#ffffff' | accentColor '#F45E43'

App.tsx onEditor registration pattern (StrictMode-guarded):
```ts
if (!editor.Blocks.get('ddroidd-<name>')) {
  editor.Blocks.add('ddroidd-<name>', <name>Block);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-author the 5 remaining sections as branded block modules</name>
  <files>
    app/client/src/blocks/new-collegues.ts,
    app/client/src/blocks/initiatives.ts,
    app/client/src/blocks/hiring.ts,
    app/client/src/blocks/want-to-know-more.ts,
    app/client/src/blocks/disclaimer.ts
  </files>
  <action>
Create one block module per section, following the EXACT shape of `hero.ts` / `projects.ts`
(BLOCK-01). Each exports a `const <name>Block = { id, label, category: 'DDROIDD', content }`.
Content is a BARE MJML fragment (no `<mjml>` root — wrapping happens at compile time).

Import ONLY `./BLOCK_DEFAULTS` (relative, no `@/` alias). Do NOT import react, grapesjs, or
anything browser-side — the verify script (Task 3) imports these modules headlessly and will
fail if they pull in the editor runtime.

For every element, inline the head.mjml defaults per element via `BLOCK_DEFAULTS` (mj-attributes
is unsupported — issue #35): every `mj-text` carries `color`, `font-family`, `font-size`,
`line-height`; every `mj-section` carries an explicit `background-color`. Never place
`mj-include`, `mj-attributes`, or `mj-style` inside a content string.

Faithfully preserve each section's content, structure (sections/columns), and the inner inline
styles that intentionally diverge from the defaults — do NOT normalize these away:
  - Section headings render as inner `<h2>`/`<h3>` at their source sizes (20px/30px, 20px/24px).
  - hiring: the role `<ul>` list keeps its yellow `#FCD400` color and `padding-left: 20px`.
  - The mj-text wrapper `color` follows BLOCK_DEFAULTS (`#ffffff`) as hero.ts/projects.ts do;
    the inner element `style` attributes carry the real per-element colors from source.

Per-section specifics:
  - new-collegues.ts (`ddroidd-new-collegues`, label 'DDROIDD New Collegues'): signature image +
    "New Collegues" heading, intro paragraph, then the colleague photo grid (3-col row + 2-col row).
    NORMALIZE the source typo `#ffffffff` (8-digit hex) → `#ffffff`. Keep all storyblok/webflow
    image URLs as-is.
  - initiatives.ts (`ddroidd-initiatives`, label 'DDROIDD Initiatives'): divider, signature +
    "MEET-UPS AND INITIATIVES" heading, then the two sub-stories (Internal Newsletter Feedback,
    Techsylvania 2026) with their links, plus the Codecamp image. Preserve the `<a>` hrefs and
    underline styling.
  - hiring.ts (`ddroidd-hiring`, label 'DDROIDD Hiring'): divider, two-column (40%/60%) layout with
    placeholder image + "WE'RE HIRING!" heading + intro + yellow role list, then the full-width
    referral-process paragraph with its mailto + sharepoint links. Preserve the 40%/60% column
    widths and the `mj-spacer height="50px"`.
  - want-to-know-more.ts (`ddroidd-want-to-know-more`, label 'DDROIDD Want To Know More'): divider,
    signature + heading, then the paragraph with the `newsletter@ddroidd.com` mailto link.
  - disclaimer.ts (`ddroidd-disclaimer`, label 'DDROIDD Disclaimer'): the confidentiality paragraph.
    DECISION (faithful re-author, documented in a file comment): source has NO section
    `background-color` and black text (`#000000`) — in the full email it renders black-on-white
    because mj-body defaults to white. Set an explicit `background-color="#ffffff"` on the section
    (satisfies the every-section-explicit-bg rule and the white-default-text trap) and KEEP the
    black italic centered text. Do NOT re-brand it dark with light text — that diverges from source.

Add a short header comment to each file (like hero.ts/projects.ts) noting it was re-authored from
the corresponding `src/sections/*.mjml`, that mj-attributes/include/style are forbidden, and any
intentional divergence (the disclaimer bg decision; the `#ffffffff` normalization).

Do NOT add local `assets/` image references — all images in these 5 sections already use hosted
absolute URLs (storyblok / webflow cdn); keep them verbatim. No structural locking flags beyond
what hero.ts/projects.ts already do (locking is deferred spike work — matches existing blocks).
  </action>
  <verify>
    <automated>test -f app/client/src/blocks/new-collegues.ts && test -f app/client/src/blocks/initiatives.ts && test -f app/client/src/blocks/hiring.ts && test -f app/client/src/blocks/want-to-know-more.ts && test -f app/client/src/blocks/disclaimer.ts && ! grep -lE 'mj-(attributes|include|style)|from .(react|grapesjs)' app/client/src/blocks/new-collegues.ts app/client/src/blocks/initiatives.ts app/client/src/blocks/hiring.ts app/client/src/blocks/want-to-know-more.ts app/client/src/blocks/disclaimer.ts</automated>
  </verify>
  <done>
5 block modules exist, each exporting a `<name>Block` object with `id`/`label`/`category: 'DDROIDD'`/`content`;
each imports only `./BLOCK_DEFAULTS`; none contain `mj-attributes`/`mj-include`/`mj-style` or import react/grapesjs;
disclaimer carries explicit `background-color="#ffffff"` with black text; `#ffffffff` normalized to `#ffffff`.
Full compile proof is Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose TEMPLATE_MJML and wire blocks + "New from Template" into App.tsx</name>
  <files>app/client/src/blocks/template.ts, app/client/src/App.tsx</files>
  <action>
Create `app/client/src/blocks/template.ts` (its OWN module — NOT inside App.tsx, so the headless
verify script can import it without dragging in grapesjs). It imports the 7 block objects
(`heroBlock`, `newColleguesBlock`, `projectsBlock`, `initiativesBlock`, `hiringBlock`,
`wantToKnowMoreBlock`, `disclaimerBlock`) — all relative imports, alias-free — and exports:

```ts
export const TEMPLATE_MJML = `<mjml>\n  <mj-body>\n${[
  heroBlock, newColleguesBlock, projectsBlock, initiativesBlock,
  hiringBlock, wantToKnowMoreBlock, disclaimerBlock,
].map((b) => b.content).join('\n')}\n  </mj-body>\n</mjml>`;
```

Order MUST match `src/pages/index.mjml`: hero, new-collegues, projects, initiatives, hiring,
want-to-know-more, disclaimer.

In `App.tsx`:
  1. Import the 5 new block modules and `TEMPLATE_MJML`.
  2. In `onEditor`, register each new block with the existing StrictMode guard pattern
     (`if (!editor.Blocks.get('ddroidd-<name>')) editor.Blocks.add('ddroidd-<name>', <name>Block);`).
     Leave the existing hero + projects registration intact.
  3. Add a `handleNewFromTemplate` handler and a "New from Template" toolbar button (next to the
     existing buttons) that calls `editor.setComponents(TEMPLATE_MJML)` then `editor.UndoManager.clear()`.
     Add a comment: this is seed-time authoring — the SAME blessed mechanism as the empty-scaffold
     seed and block drops — NOT the forbidden "reload persisted state from an MJML string" pattern
     (persisted state stays project JSON; see `.claude/rules/grapesjs.md`).
  4. Do NOT change the empty-canvas default: the existing
     `if (editor.getComponents().length === 0) { editor.setComponents('<mjml><mj-body></mj-body></mjml>'); ... }`
     guard MUST remain — the full template loads ONLY on the button click, never auto-seeded.
  </action>
  <verify>
    <automated>grep -q "TEMPLATE_MJML" app/client/src/blocks/template.ts && grep -q "ddroidd-disclaimer" app/client/src/App.tsx && grep -q "setComponents(TEMPLATE_MJML)" app/client/src/App.tsx && grep -q "getComponents().length === 0" app/client/src/App.tsx && ! grep -qE "from .(react|grapesjs)" app/client/src/blocks/template.ts</automated>
  </verify>
  <done>
`template.ts` exports `TEMPLATE_MJML` (7 blocks, index.mjml order, alias-free, no grapesjs import).
App.tsx registers all 5 new blocks under the DDROIDD category, has a working "New from Template" button
that `setComponents(TEMPLATE_MJML)` + `UndoManager.clear()`, and preserves the empty-scaffold default seed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Headless mjml@4.18.0 compile gate for every block + the template</name>
  <files>app/server/scripts/verify-blocks.ts, app/server/package.json</files>
  <action>
Create `app/server/scripts/verify-blocks.ts` (in `scripts/`, NOT `src/` — the server `build: tsc`
must not try to compile this cross-package import). It runs in server context so it resolves
`mjml@4.18.0` from `app/server/node_modules`, and imports the block content strings via relative
paths from `../../client/src/blocks/*` (tsx resolves `.ts`; the block modules are alias-free and
browser-free, so this import succeeds headlessly).

The script must:
  1. Import `mjml2html` from `mjml`, the 5 new block objects + `heroBlock` + `projectsBlock`, and
     `TEMPLATE_MJML`.
  2. For each of the 7 blocks: wrap its `.content` exactly as `compile.ts` does — mirror the
     production conditional (`/<mjml/i.test(trimmed)` → use as-is else wrap in
     `<mjml>\n  <mj-body>\n    ${trimmed}\n  </mj-body>\n</mjml>`). Compile with
     `{ validationLevel: 'soft', minify: false }` (identical options to the server).
  3. Also compile `TEMPLATE_MJML` directly (it already has the `<mjml>` root).
  4. GATE per compile: PASS iff `result.errors.length === 0` AND `result.html` is a non-empty string.
     The non-empty check specifically guards the issue #194 silent-empty-document failure.
  5. Print a per-target line (name → PASS/FAIL, error count). Record (print) every entry of each
     `result.errors` array as a warning line even on PASS ("warnings recorded"). With
     `validationLevel: 'soft'` mjml2html never throws, so the gate is the errors/html check, not a catch.
  6. Exit `process.exit(1)` if ANY target fails the gate; exit `0` only if all 8 targets
     (7 blocks isolated + the full template) pass.

Add an npm script to `app/server/package.json`: `"verify:blocks": "tsx scripts/verify-blocks.ts"`.
  </action>
  <verify>
    <automated>npm run verify:blocks --prefix app/server</automated>
  </verify>
  <done>
`npm run verify:blocks --prefix app/server` exits 0 and reports PASS for all 8 targets
(hero, new-collegues, projects, initiatives, hiring, want-to-know-more, disclaimer, + full template),
each with `errors.length === 0` and non-empty HTML. Any `errors` entries are printed as recorded warnings.
</done>
</task>

</tasks>

<verification>
- `npm run verify:blocks --prefix app/server` exits 0; all 8 targets PASS (errors=0, html non-empty).
- No block or template module contains `mj-attributes` / `mj-include` / `mj-style`.
- No block or template module imports react or grapesjs (verify script stays headless-safe).
- App.tsx registers 7 DDROIDD blocks and keeps the empty `<mjml><mj-body>` scaffold as the default.
- "New from Template" button seeds `TEMPLATE_MJML` then clears the undo stack (button-click only).

Runtime (human, editor) checks are OUT OF SCOPE for this quick task and remain gated behind the
Phase-1 human runtime check per STATE.md — the automated compile gate is the deliverable here.
</verification>

<success_criteria>
- 5 new branded block modules re-authored faithfully from source, defaults inlined via BLOCK_DEFAULTS.
- `TEMPLATE_MJML` composes all 7 sections in index.mjml order in its own alias-free module.
- App.tsx exposes all 7 blocks in the panel and a working "New from Template" path without auto-seeding.
- Every block compiles cleanly in isolation and the full template compiles cleanly, via server mjml@4.18.0.
</success_criteria>

<output>
Executor does NOT commit (no-commit rule). On completion, report:
- Changed/created files list.
- Suggested commit message (developer commits manually).
Create `.planning/quick/260704-p2a-re-author-latest-newsletter-index-mjml-s/260704-p2a-SUMMARY.md` when done.
</output>
