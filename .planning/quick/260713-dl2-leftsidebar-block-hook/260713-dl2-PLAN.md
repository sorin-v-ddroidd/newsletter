---
phase: quick
plan: 260713-dl2
type: execute
wave: 1
depends_on: []
files_modified:
  - app/client/src/editor/hooks/useBlockSearch.ts
  - app/client/src/editor/LeftSidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Typing in the block search box filters chips by label text exactly as before"
    - "Categories with zero matching blocks after filtering disappear from the list"
    - "The DDROIDD category always renders first among non-empty categories"
    - "LeftSidebar.tsx's renderBlocks() contains no inline filter/sort/stripHtml logic"
  artifacts:
    - path: "app/client/src/editor/hooks/useBlockSearch.ts"
      provides: "useBlockSearch hook: (mapCategoryBlocks, query) -> filtered+sorted [category, Block[]][] "
      exports: ["useBlockSearch"]
    - path: "app/client/src/editor/LeftSidebar.tsx"
      provides: "renderBlocks() calls useBlockSearch instead of inlining filter/sort"
  key_links:
    - from: "app/client/src/editor/LeftSidebar.tsx"
      to: "app/client/src/editor/hooks/useBlockSearch.ts"
      via: "import + hook call inside renderBlocks() render-prop callback"
      pattern: "useBlockSearch\\("
---

<objective>
Extract the block search/filter/sort logic currently inlined in `LeftSidebar.tsx`'s `renderBlocks()` into a dedicated custom hook, `app/client/src/editor/hooks/useBlockSearch.ts`, per `.claude/rules/component-patterns.md` ("Custom Hooks" — non-visual stateful/derived logic must not live inline in a component body).

Purpose: `renderBlocks()` currently does non-visual work (map -> filter by query -> drop empty categories -> sort brand-first) inline inside the JSX render tree. This violates the project's custom-hook convention and makes the render function harder to read. `useSelectedComponent.ts` and `useLayerVisibility.ts` (already imported into this same file) establish the pattern: one hook per concern, colocated in `editor/hooks/`.

Output: A new `useBlockSearch` hook exporting a pure derivation function-as-hook (no side effects/subscriptions needed — this is derived state, not an event subscription, but it still qualifies for hook extraction because it is non-visual, reusable-shaped, stateful-input logic per the rule's intent and explicit user request). `LeftSidebar.tsx` updated so `renderBlocks()` only calls the hook and maps the result to JSX — zero inline filter/sort/stripHtml calls remain in the component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/rules/component-patterns.md
@.claude/rules/react-patterns.md
@.claude/rules/code-style.md
@.claude/rules/gotchas.md

<interfaces>
<!-- Exact current logic being moved, extracted from app/client/src/editor/LeftSidebar.tsx.
     Preserve behavior identically -- this is a pure refactor. -->

Current inline block (LeftSidebar.tsx, inside BlocksProvider render-prop, to be replaced):
```
const q = query.trim().toLowerCase();
const categories = Array.from(mapCategoryBlocks)
  .map(([category, blocks]) => {
    const filtered = q
      ? blocks.filter((b) => stripHtml(b.getLabel()).toLowerCase().includes(q))
      : blocks;
    return [category, filtered] as const;
  })
  .filter(([, blocks]) => blocks.length > 0)
  .sort(([a], [b]) => {
    if (a === BRAND_CATEGORY) return -1;
    if (b === BRAND_CATEGORY) return 1;
    return 0;
  });
```

`stripHtml` helper (module-level in LeftSidebar.tsx, moves into the hook file since it's only used there):
```
const stripHtml = (html: string) => html.replace(/<[^>]*>/g, '').trim();
```

`BRAND_CATEGORY` constant (module-level in LeftSidebar.tsx):
```
const BRAND_CATEGORY = 'DDROIDD';
```
Decision: keep `BRAND_CATEGORY` exported from the hook module as the single source of truth (LeftSidebar.tsx still needs it to know which category badge renders "On-brand" — import it from the hook file rather than duplicating the string).

Provider shape this hook's input must match exactly (from `@grapesjs/react` BlocksProvider.d.ts) — do not alter, this is the render-prop contract `gotchas.md`/`grapesjs.md` say not to fight:
```
export type MapCategoryBlocks = Map<string, Block[]>;
```
`mapCategoryBlocks`, `dragStart`, `dragStop` come from `BlocksProvider`'s children render-prop and must continue to be consumed exactly as today — only the derivation of the rendered category list changes.

Existing hook pattern to match (colocation + JSDoc-free inline comment style, no side-effect subscription needed here since this hook has no event source — it's pure derivation from props):
- app/client/src/editor/hooks/useSelectedComponent.ts
- app/client/src/editor/hooks/useLayerVisibility.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useBlockSearch hook</name>
  <files>app/client/src/editor/hooks/useBlockSearch.ts</files>
  <action>
Create `app/client/src/editor/hooks/useBlockSearch.ts`. Export `BRAND_CATEGORY` (string constant `'DDROIDD'`) and `useBlockSearch`.

`useBlockSearch(mapCategoryBlocks: MapCategoryBlocks, query: string): Array<readonly [string, Block[]]>` — a plain function (no `useState`/`useEffect` needed since it has no internal state or subscription; it's a pure derivation over its arguments, which is exactly the class of logic `component-patterns.md` says belongs in a hook rather than inline in a component body). Still name it with the `use` prefix per convention and treat it as the hook responsible for this concern.

Move `stripHtml` into this file as a local (non-exported) helper — it's only used by the filter logic now.

Implementation must reproduce the current behavior exactly:
1. Lowercase-trim `query` once.
2. `Array.from(mapCategoryBlocks)` → for each `[category, blocks]`, filter `blocks` by `stripHtml(b.getLabel()).toLowerCase().includes(q)` when `q` is non-empty, otherwise keep `blocks` unchanged.
3. Drop categories whose filtered `blocks` array is empty.
4. Sort so `BRAND_CATEGORY` sorts first (same comparator: return -1 if `a === BRAND_CATEGORY`, 1 if `b === BRAND_CATEGORY`, else 0 — stable no-op for all other pairs).
5. Return the resulting `[category, Block[]][]`.

Import `type { Block }` from `'grapesjs'` (matches LeftSidebar.tsx's existing import). `MapCategoryBlocks` is NOT re-exported from `@grapesjs/react`'s package root -- verified `dist/index.d.ts` only re-exports `BlocksProvider` (default) and `BlocksResultProps`. Import it directly from the subpath: `import type { MapCategoryBlocks } from '@grapesjs/react/dist/BlocksProvider'`.

Follow `code-style.md`: `type` over `interface`, no `let` unless required, destructure at top where applicable. Add a short inline comment (not JSDoc — this hook is small/self-evident per `jsdoc.md`'s "skip for one-off internal components/types whose name+shape already say everything", but the *why* — pure derivation, not a subscription hook — is worth one line) explaining why this hook has no `useEffect`/subscription unlike its siblings in the same folder.
  </action>
  <verify>
    <automated>MISSING — no test scaffold exists for editor hooks yet; verify via `npx tsc --noEmit` from app/client and manual read-through against the reproduced logic above</automated>
  </verify>
  <done>`useBlockSearch.ts` exists, exports `useBlockSearch` and `BRAND_CATEGORY`, compiles with no TypeScript errors, and its output logic is line-for-line equivalent to the current inline block in LeftSidebar.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LeftSidebar.tsx to the new hook</name>
  <files>app/client/src/editor/LeftSidebar.tsx</files>
  <action>
Update `app/client/src/editor/LeftSidebar.tsx`:
1. Remove the module-level `BRAND_CATEGORY` constant and `stripHtml` helper (both now live in the hook file).
2. Import `useBlockSearch` and `BRAND_CATEGORY` from `./hooks/useBlockSearch`, alongside the existing `useSelectedComponent`/`useLayerVisibility` imports (same relative-import style, no `@/*` alias since this is intra-editor-folder — match how the other two hooks are imported).
3. Inside `renderBlocks()`'s `BlocksProvider` render-prop callback, replace the inline `q`/`categories` derivation block with a single call: `const categories = useBlockSearch(mapCategoryBlocks, query);`.

Note: `useBlockSearch` is called inside a render-prop callback passed as `children` to `<BlocksProvider>`, not directly in the component body. Since `useBlockSearch` here does not call `useState`/`useEffect` internally (it's a pure function per Task 1), calling it from within the render-prop callback is safe and does not violate the Rules of Hooks position-invariance requirement in the way a stateful hook would — confirm this by checking the final hook implementation has zero internal hook calls. If `useBlockSearch` internally needs no React hooks at all, this is fine as-is; if it turns out during Task 1 that some internal memoization hook is added, move the `useBlockSearch(...)` call up to the top of the `LeftSidebar` component body instead, and pass the already-computed `categories` down into the render-prop closure, to keep it unconditionally called at the top level.
4. Leave the rest of `renderBlocks()` (the JSX mapping over `categories` to render `BlockChip`s, the "No blocks match" empty state, the `dragStart`/`dragStop`/`mapCategoryBlocks` provider destructuring) untouched — same variable name `categories`, same shape, so the JSX below needs no changes.
5. Do not touch `LayerItem`, `BlockChip`, `renderLayers()`, or any Layers-tab code.
  </action>
  <verify>
    <automated>cd app/client && npx tsc --noEmit</automated>
  </verify>
  <done>LeftSidebar.tsx has no inline filter/sort/stripHtml logic in its body; `renderBlocks()` calls `useBlockSearch(mapCategoryBlocks, query)` and renders its result; `npx tsc --noEmit` passes with no new errors; `npm run dev` (or existing dev script) shows the Blocks tab rendering all 22 chips with DDROIDD category first and search still filtering by label text, identical to pre-refactor behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side refactor of existing derived-state logic; no new input, network, or trust boundary is introduced. `block.getLabel()`/`getMedia()` HTML trust (dangerouslySetInnerHTML) is pre-existing and untouched by this plan (owned by BlockChip, out of scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | useBlockSearch.ts (new file) | accept | Pure refactor, no new external input surface; existing GrapesJS block labels are already a trusted/first-party data source per grapesjs.md, unchanged by this plan. |
</threat_model>

<verification>
1. `cd app/client && npx tsc --noEmit` passes with no new type errors.
2. Manual read-through confirms `useBlockSearch`'s filter/sort logic is byte-for-byte equivalent to the removed inline block (same comparator, same filter predicate, same empty-category drop).
3. Run the app locally (`npm run dev`), open the Blocks tab: confirm all block chips render, DDROIDD category appears first, typing a query filters chips and hides empty categories, clearing the query restores the full list.
4. Confirm no inline `.filter(`/`.sort(`/`stripHtml` calls remain inside `LeftSidebar.tsx`'s `renderBlocks()`.
</verification>

<success_criteria>
- `useBlockSearch.ts` created in `app/client/src/editor/hooks/`, exporting `useBlockSearch` and `BRAND_CATEGORY`, matching the colocation pattern of `useSelectedComponent.ts`/`useLayerVisibility.ts`.
- `LeftSidebar.tsx`'s `renderBlocks()` contains zero inline filter/sort/stripHtml logic — only a call to `useBlockSearch` and JSX rendering of its result.
- No behavior change: search filtering, empty-category removal, and DDROIDD-first sorting all work identically to before the refactor.
- `BlockChip`, `LayerItem`, and the Layers tab are untouched.
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/quick/260713-dl2-leftsidebar-block-hook/260713-dl2-SUMMARY.md` when done
</output>
</content>
