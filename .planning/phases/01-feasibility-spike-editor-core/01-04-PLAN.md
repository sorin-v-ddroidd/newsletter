---
phase: 01-feasibility-spike-editor-core
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - app/client/src/experiments/mjAttributes.ts
  - .planning/phases/01-feasibility-spike-editor-core/CRITERION-5-FINDINGS.md
autonomous: true
requirements: [EDIT-05]

must_haves:
  truths:
    - "Negative confirmation (Part A): mj-attributes defaults are confirmed silently dropped — text does NOT inherit the mj-attributes color in compiled output"
    - "Positive validation (Part B): the ddroidd-hero block with inlined BLOCK_DEFAULTS survives the localStorage round-trip and compiles correctly with NO mj-head injection"
    - "The criterion-5 checklist is answered in writing: fluid-on-mobile round-trip + compile survival, background-url round-trip + compile survival, compile warnings, and getHtml() shape"
  artifacts:
    - path: ".planning/phases/01-feasibility-spike-editor-core/CRITERION-5-FINDINGS.md"
      provides: "Written documentation of the mj-attributes experiment and the BLOCK_DEFAULTS mitigation validation"
      contains: "BLOCK_DEFAULTS"
    - path: "app/client/src/experiments/mjAttributes.ts"
      provides: "The Part A negative-test MJML string and the experiment runner/console procedure"
      contains: "mj-attributes"
  key_links:
    - from: "app/client/src/experiments/mjAttributes.ts"
      to: "/api/compile"
      via: "POST of experiment MJML for compile comparison"
      pattern: "/api/compile|compile"
---

<objective>
Run the criterion #5 mj-attributes experiment and document the findings: (Part A) confirm `mj-attributes` defaults are silently dropped, and (Part B) confirm the `ddroidd-hero` block with all defaults inlined via BLOCK_DEFAULTS survives the round-trip and compiles correctly with NO mj-head injection. Answer the full criterion-5 checklist (fluid-on-mobile, background-url, compile warnings, getHtml shape).

Purpose: Validate the BLOCK_DEFAULTS mitigation strategy — the architectural foundation for every branded block in Phase 3. This is ROADMAP success criterion #5 and resolves open questions #3 (background-url) and #4 (fluid-on-mobile).
Output: A written `CRITERION-5-FINDINGS.md` documenting both experiment parts and the checklist answers, plus a small reusable experiment helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike-editor-core/SKELETON.md
@.planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-editor-core/01-PATTERNS.md
@.planning/phases/01-feasibility-spike-editor-core/01-01-SUMMARY.md
@.planning/phases/01-feasibility-spike-editor-core/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Experiment design (01-RESEARCH.md lines 441-491):
- Part A negative MJML (lines 450-462): a minimal `<mjml>` with `<mj-head><mj-attributes><mj-text color="#ff0000"/></mj-attributes></mj-head>` and a body mj-text. Expected: text does NOT render red — mj-attributes dropped.
- Part B positive (lines 469-481): drop ddroidd-hero on a fresh canvas → round-trip → getHtml() → POST /api/compile with NO mj-head → verify white text, Calibri stack, #0B1624 background from inlined defaults alone.

Criterion-5 checklist (01-RESEARCH.md lines 483-491):
- Does fluid-on-mobile on mj-image survive getProjectData/loadProjectData?
- Does fluid-on-mobile appear in compiled HTML?
- Does background-url on mj-section survive round-trip as an attribute?
- Does background-url appear in compiled HTML?
- Any MJML compile warnings/errors?
- What does console.log(editor.getHtml()) show — fragment or full <mjml>?

Hero block (from Plan 01) carries both background-url and fluid-on-mobile probes. Compile endpoint (from Plan 02) at /api/compile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Part A — confirm mj-attributes silently dropped</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 445-467 Part A procedure; lines 523-528 Pitfall 4 mj-attributes dropped)
    - CLAUDE.md (component matrix — mj-attributes = NO; "What NOT to Use")
  </read_first>
  <action>
    Create `app/client/src/experiments/mjAttributes.ts` holding the Part A negative-test MJML string (a full `<mjml>` doc with `<mj-head><mj-attributes><mj-text color="#ff0000"/></mj-attributes></mj-head>` and a body mj-text reading "Test") and a small runner that POSTs this MJML to `/api/compile` and logs whether the compiled HTML applies `#ff0000` to the body text. Run it and observe: the expected result is that the text does NOT inherit `#ff0000` (mj-attributes dropped). Record the observed compiled-text color. Note: this is server-side compile confirmation of the architectural limitation; do not import the mj-attributes MJML into the editor canvas (setComponents is lossy and not the point).
  </action>
  <acceptance_criteria>
    - `app/client/src/experiments/mjAttributes.ts` contains the Part A MJML with `mj-attributes` and `color="#ff0000"` (grep)
    - The runner's output records the compiled body-text color, and the finding (dropped vs applied) is captured for CRITERION-5-FINDINGS.md
    - Expected and documented: mj-attributes color is NOT applied to the body text in compiled HTML (confirms the limitation)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Part B + checklist + write CRITERION-5-FINDINGS.md</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 469-491 Part B + checklist; lines 770-778 open questions #3/#4)
    - .planning/phases/01-feasibility-spike-editor-core/01-PATTERNS.md (lines 103-149 hero divergences — background-url + fluid-on-mobile probes)
    - .planning/phases/01-feasibility-spike-editor-core/01-01-SUMMARY.md (round-trip result) and 01-03-SUMMARY.md if present (getHtml shape)
  </read_first>
  <action>
    Part B positive validation: drop the `ddroidd-hero` block on a fresh canvas, run the localStorage round-trip (save → load), assert the canvas state is byte-identical, then run `editor.getHtml()` → POST `/api/compile` (NO mj-head injected server-side — the default endpoint) and inspect the compiled HTML. Confirm white text (`#ffffff`), the Calibri/Roboto font stack, and the `#0B1624` background all come through from inlined BLOCK_DEFAULTS alone. Then answer the full criterion-5 checklist by inspecting the round-tripped project JSON and the compiled HTML: whether `fluid-on-mobile` survives round-trip and appears in compiled output; whether `background-url` survives round-trip and appears in compiled output; any compile warnings; and the `getHtml()` shape (run `console.log(editor.getHtml())` here directly — do not depend on Plan 03's summary existing, since 03 and 04 run in the same wave). Write all findings — Part A result, Part B result, and the six checklist answers — into `.planning/phases/01-feasibility-spike-editor-core/CRITERION-5-FINDINGS.md`, concluding with an explicit "BLOCK_DEFAULTS mitigation validated" (or, if it failed, what broke and the implication for Phase 3).
  </action>
  <acceptance_criteria>
    - `CRITERION-5-FINDINGS.md` exists and answers all six checklist items (fluid-on-mobile round-trip + compile; background-url round-trip + compile; compile warnings; getHtml shape) with concrete observed values
    - The Part B compiled HTML for the hero block shows `#ffffff` text, the Calibri/Roboto stack, and `#0B1624` background produced WITHOUT any mj-head injection (documented)
    - `CRITERION-5-FINDINGS.md` states explicitly whether the BLOCK_DEFAULTS mitigation is validated
    - SUMMARY links to CRITERION-5-FINDINGS.md and states the criterion-5 PASS/FAIL outcome
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/compile | Reuses the Plan 02 boundary; the experiment POSTs MJML to the existing endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | POST /api/compile body | mitigate | Inherited from Plan 02 (`express.json({ limit: '1mb' })`). |
| T-01-02 | Spoofing | CORS | mitigate | Inherited from Plan 02 (`cors({ origin: 'http://localhost:5173' })`). |

No new trust boundary — this plan is an experiment against the Plan 02 endpoint. See Plan 02 (`01-02`) for the substantive threat model.
</threat_model>

<verification>
- Part A: mj-attributes confirmed dropped (compiled text not red).
- Part B: hero with inlined BLOCK_DEFAULTS compiles correctly with no mj-head.
- Criterion-5 checklist fully answered in writing; background-url + fluid-on-mobile survival documented.
</verification>

<success_criteria>
- ROADMAP criterion 5 (mj-attributes experiment + BLOCK_DEFAULTS validation) documented.
- Open questions #3 (background-url) and #4 (fluid-on-mobile) resolved with observed data.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike-editor-core/01-04-SUMMARY.md` when done, plus the `CRITERION-5-FINDINGS.md` deliverable. Record the criterion-5 PASS/FAIL outcome and the mitigation-validated conclusion.
</output>
