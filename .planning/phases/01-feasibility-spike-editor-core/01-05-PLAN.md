---
phase: 01-feasibility-spike-editor-core
plan: 05
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - .planning/phases/01-feasibility-spike-editor-core/CLIENT-RENDER-GATE.md
autonomous: false
requirements: [EDIT-01]

must_haves:
  truths:
    - "Before any client-render attempt, the user has confirmed whether a machine with CLASSIC Outlook (Office 365 MSI / 2019 / 2021) is available — the dev machine's new Outlook Store app has NO COM and blocks both PowerShell scripts"
    - "dist/spike-output.html either renders correctly in Outlook AND Gmail (no broken fonts, no dark-on-dark text, no spacing failures), OR a partial pass is documented (browser render verified) with the client-render gate deferred and a recorded follow-up"
  artifacts:
    - path: ".planning/phases/01-feasibility-spike-editor-core/CLIENT-RENDER-GATE.md"
      provides: "Documented criterion-4 outcome: full pass (Outlook+Gmail) or partial pass with deferred follow-up"
      contains: "criterion"
  key_links:
    - from: "dist/spike-output.html"
      to: "QuickEmailTest.ps1 / EmailTester.ps1"
      via: "PowerShell client-render verification (D-05)"
      pattern: "spike-output.html"
---

<objective>
Run the criterion #4 client-render gate (D-05): verify the compiled `dist/spike-output.html` renders correctly in Outlook and Gmail using the existing PowerShell + Outlook COM scripts — OR, if classic Outlook is unavailable, document a partial pass (browser render verified) and defer the client-render gate with a recorded follow-up.

Purpose: This is the final exit gate before declaring the spike viable. It is BLOCKED on the dev machine (new Outlook Store app has no COM — both scripts fail at COM object creation), so a human checkpoint MUST gate it.
Output: `CLIENT-RENDER-GATE.md` documenting either a full client-render pass or a partial pass with a deferred follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike-editor-core/SKELETON.md
@.planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-editor-core/01-03-SUMMARY.md
@QuickEmailTest.ps1
@EmailTester.ps1

<interfaces>
D-05 verification commands (01-CONTEXT.md lines 36-37; 01-RESEARCH.md lines 695-702):
- Outlook preview: `.\QuickEmailTest.ps1 -HtmlFilePath dist\spike-output.html -PreviewOnly`
- Gmail send: `.\EmailTester.ps1 -HtmlFilePath dist\spike-output.html -TestEmails sorin.vieriu@ddroidd.com`

BLOCKER (01-RESEARCH.md lines 530-534, 638-648): BOTH scripts call `New-Object -ComObject Outlook.Application` (QuickEmailTest.ps1 line 7; EmailTester.ps1 lines 43, 153). The dev machine has new Outlook for Windows (Store app, no COM). Both scripts fail immediately. Criterion #4 is fully blocked on the dev machine — needs a machine with classic Outlook (Office 365 MSI / 2019 / 2021).

PASS = no broken fonts, no dark-on-dark text, no spacing failures in Outlook AND Gmail.
Partial pass fallback (01-VALIDATION.md line 68): browser render of dist/spike-output.html verified; client-render gate deferred with a recorded follow-up.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Checkpoint — confirm classic Outlook availability</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 530-534 Pitfall 5 both scripts need COM; lines 638-648 Environment Availability — no COM on dev machine)
  </read_first>
  <action>
    PAUSE for the user. This is a blocking human checkpoint that MUST run before any client-render attempt. The D-05 gate is blocked on the dev machine because both PowerShell scripts require Outlook COM, which the new Outlook for Windows Store app does not provide. Ask the user whether a machine with classic Outlook (Office 365 MSI / Office 2019 / Office 2021) is available, and record their answer plus the chosen path (full PowerShell verification vs documented partial pass) before Task 2 proceeds.
  </action>
  <what-built>
    The Walking Skeleton produced `dist/spike-output.html` (Plan 03). The D-05 client-render gate uses `QuickEmailTest.ps1` (Outlook preview) and `EmailTester.ps1` (Gmail send) — but BOTH require Outlook COM, which the dev machine's new Outlook Store app does NOT provide. Both scripts fail immediately at `New-Object -ComObject Outlook.Application`.
  </what-built>
  <how-to-verify>
    1. Confirm: do you have access to a machine with CLASSIC Outlook installed (Office 365 MSI / Office 2019 / Office 2021 — NOT the new Outlook for Windows Store app)?
    2. If YES: copy `dist/spike-output.html` to that machine (or run from this repo there) and proceed to Task 2.
    3. If NO: choose a fallback — (a) manually paste the HTML body into Outlook Web App, (b) use a hosted testing service (Litmus — out of scope but viable), or (c) accept a PARTIAL pass: browser render of `dist/spike-output.html` verified, client-render gate deferred with a follow-up recorded. State which path you choose.
  </how-to-verify>
  <acceptance_criteria>
    - The user has explicitly stated whether classic Outlook is available and, if not, which fallback path is chosen
    - The chosen path (full PS verification vs documented partial pass) is recorded before Task 2 runs
  </acceptance_criteria>
  <resume-signal>Type "outlook available" (proceed to PS scripts), or "partial pass" (document browser-only render + defer), or describe the chosen fallback.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run client-render verification (or document partial pass)</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 695-702 Criterion 4 procedure; lines 530-534 Pitfall 5; lines 760-763 open question #1)
    - QuickEmailTest.ps1 (Outlook preview script — confirm COM line)
    - EmailTester.ps1 (Gmail send script — confirm COM lines)
  </read_first>
  <action>
    Branch on the Task 1 outcome. If classic Outlook is available: run `.\QuickEmailTest.ps1 -HtmlFilePath dist\spike-output.html -PreviewOnly` and inspect the Outlook draft, then run `.\EmailTester.ps1 -HtmlFilePath dist\spike-output.html -TestEmails sorin.vieriu@ddroidd.com` and inspect the message in the Gmail inbox; verify no broken fonts, no dark-on-dark text, and no spacing failures in BOTH clients. If classic Outlook is NOT available (partial-pass path): confirm `dist/spike-output.html` renders correctly in a browser (white text on `#0B1624`, Calibri/Roboto stack, correct spacing) and record a deferred follow-up for the Outlook/Gmail gate. Either way, write the outcome into `.planning/phases/01-feasibility-spike-editor-core/CLIENT-RENDER-GATE.md`: state full-pass or partial-pass, the clients actually checked, any rendering defects found, and (if partial) the explicit follow-up action and where the next classic-Outlook check should happen.
  </action>
  <acceptance_criteria>
    - `CLIENT-RENDER-GATE.md` exists and records: the verification path taken, which clients were checked, any defects, and the criterion-4 outcome (full pass / partial pass + deferred follow-up)
    - If full pass: documented confirmation of no broken fonts, no dark-on-dark text, no spacing failures in Outlook AND Gmail
    - If partial pass: documented browser render confirmation plus an explicit deferred follow-up for the client-render gate
    - SUMMARY states the criterion-4 outcome and, if deferred, adds the follow-up to carry into Phase 2/STATE.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs local PowerShell against a static HTML file and reads results. No new network or application trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | accept | No new trust boundary; the compile-endpoint threats (T-01-01, T-01-02) are owned by Plan 02 (`01-02`). This is a developer-toolchain verification step, not part of the deployed app. |
</threat_model>

<verification>
- Human checkpoint confirms classic-Outlook availability BEFORE any render attempt.
- Criterion 4 resolved: either full Outlook+Gmail pass or a documented partial pass with deferred follow-up.
</verification>

<success_criteria>
- ROADMAP criterion 4 (client render) either fully verified or explicitly deferred with a recorded follow-up — the spike is not silently declared complete on this criterion.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike-editor-core/01-05-SUMMARY.md` when done, plus the `CLIENT-RENDER-GATE.md` deliverable. If criterion 4 is a partial pass, record the deferred follow-up so it carries into Phase 2.
</output>
