---
phase: 01-feasibility-spike-editor-core
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - app/client/src/blocks/projects.ts
  - app/client/src/App.tsx
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]

must_haves:
  truths:
    - "editor.getHtml() return shape is documented (fragment vs full <mjml>) — console.log is the literal first step"
    - "The DDROIDD Projects branded block appears in the panel and drops onto the canvas"
    - "All 5 generic block types AND both branded blocks (hero, projects) drag onto the canvas and two blocks can be reordered by drag"
    - "An mj-text block can be edited inline on the canvas; the edit is reversible via undo (Ctrl+Z) and re-applicable via redo (Ctrl+Y)"
    - "editor.getHtml() POSTed to /api/compile returns client-safe HTML written to dist/spike-output.html"
  artifacts:
    - path: "app/client/src/blocks/projects.ts"
      provides: "ddroidd-projects block definition (divider + signature header + project entry), BLOCK_DEFAULTS inlined"
      contains: "ddroidd-projects"
    - path: "app/client/src/App.tsx"
      provides: "projects block registration (guarded), compileDraft fetch to /api/compile, write to dist/spike-output.html"
      contains: "/api/compile"
    - path: "dist/spike-output.html"
      provides: "Compiled HTML output for the client-render gate (Plan 05) and browser render check"
      contains: "<!doctype html"
  key_links:
    - from: "app/client/src/App.tsx"
      to: "/api/compile"
      via: "fetch POST of editor.getHtml()"
      pattern: "fetch.*api/compile|/api/compile"
    - from: "app/client/src/blocks/projects.ts"
      to: "app/client/src/blocks/BLOCK_DEFAULTS.ts"
      via: "import of inlined defaults"
      pattern: "BLOCK_DEFAULTS"
    - from: "app/client/src/App.tsx"
      to: "app/client/src/blocks/projects.ts"
      via: "guarded Blocks.add registration"
      pattern: "ddroidd-projects"
---

<objective>
Add the second branded block (DDROIDD Projects), wire the client → server compile path (extracting MJML via `editor.getHtml()` and POSTing to `/api/compile`), and run the full editor verification: all 5 generic + both branded blocks drag/drop/reorder, inline text editing, and undo/redo. This completes the Walking Skeleton's end-to-end path.

Purpose: Demonstrate the full editor capability set (EDIT-01..05) and prove the editor → compile path works end-to-end, producing `dist/spike-output.html` for the Plan 05 client-render gate. The literal first action documents the LOW-confidence `editor.getHtml()` return shape (open question #2).
Output: A second branded block on the canvas, a documented `getHtml()` shape, and compiled HTML written to file — verified to render in a browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike-editor-core/SKELETON.md
@.planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md
@.planning/phases/01-feasibility-spike-editor-core/01-PATTERNS.md
@.planning/phases/01-feasibility-spike-editor-core/01-01-SUMMARY.md
@.planning/phases/01-feasibility-spike-editor-core/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
projectsBlock content (01-PATTERNS.md lines 224-285): id 'ddroidd-projects', label 'DDROIDD Projects', category 'DDROIDD'; THREE mj-sections (dashed divider; signature image + "Projects" h2; project logo + body text); BLOCK_DEFAULTS inlined on every mj-text wrapper and inner style; placeholder body copy (not the Elsevier text); fluid-on-mobile on signature image; bare MJML fragment.

getHtml() shape (01-RESEARCH.md lines 329-343, 506-514, 593-602): UNKNOWN — `console.log(editor.getHtml())` MUST be the literal first step. Document whether it starts with `<mjml`. The server conditional wrap (Plan 02) handles either case.

compileDraft (01-RESEARCH.md lines 604-619): `editor.getHtml()` → `fetch('/api/compile', { method:'POST', body: JSON.stringify({ mjml }) })` → `{ html, errors }`. Write `html` to `dist/spike-output.html`.

Existing branded block + registration pattern already in App.tsx from Plan 01 (guarded `editor.Blocks.get(id)` + `Blocks.add`); App.tsx already mounts GjsEditor and has localStorage helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document getHtml() shape + author projects block + register it</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-PATTERNS.md (lines 152-285 projects block concrete string; lines 372-398 guard/key rules)
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 329-343, 506-514, 593-602 getHtml shape; lines 485-490 criterion-5 checklist references getHtml shape)
    - src/sections/projects.mjml (re-author source — multi-column layout)
    - app/client/src/App.tsx (existing mount + hero registration from Plan 01)
  </read_first>
  <action>
    FIRST: with the editor mounted and at least one block on the canvas, run `console.log(editor.getHtml())` and record whether the output starts with `<mjml` or is a bare fragment — this is the literal first step and resolves open question #2; capture the result for the SUMMARY and the criterion-5 checklist. Then create `app/client/src/blocks/projects.ts` exporting `projectsBlock` (id `ddroidd-projects`, label `DDROIDD Projects`, category `DDROIDD`) as a bare MJML fragment with the three mj-sections per 01-PATTERNS.md lines 224-285: a dashed white divider, a left-aligned signature image (with `fluid-on-mobile`) plus an uppercase "Projects" h2, and a project entry (logo image + placeholder body text). Inline all BLOCK_DEFAULTS values on each mj-text wrapper and inner `style` (use `#ffffff` on wrappers, not the source's `#F45E43` accent); no `mj-attributes`/`mj-include`/`mj-style` in the string. In `app/client/src/App.tsx`, register the projects block in the existing `onEditor` callback guarded by `if (!editor.Blocks.get('ddroidd-projects'))`.
  </action>
  <acceptance_criteria>
    - SUMMARY records the `console.log(editor.getHtml())` result: starts-with-`<mjml` = yes/no, plus the first ~200 chars
    - `app/client/src/blocks/projects.ts` content contains three `<mj-section`, references `BLOCK_DEFAULTS`, contains `fluid-on-mobile`, and contains NO `mj-attributes`/`mj-include`/`mj-style`
    - `app/client/src/App.tsx` registers `ddroidd-projects` with an `editor.Blocks.get('ddroidd-projects')` guard (grep)
    - With `npm run dev:client` running, a "DDROIDD Projects" block appears in the panel and drops onto the canvas (manual observation — spike exception)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire client→server compile + write dist/spike-output.html</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 604-619 compileDraft; lines 695-702 Criterion 4 compile-and-write procedure)
    - .planning/phases/01-feasibility-spike-editor-core/01-02-SUMMARY.md (compile endpoint contract + curl results)
    - app/client/src/App.tsx (existing helpers from Plan 01)
  </read_first>
  <action>
    In `app/client/src/App.tsx`, add a `compileDraft` flow and a "Compile" button: call `editor.getHtml()`, POST `{ mjml }` to `/api/compile` (through the Vite `/api` proxy to :3000), receive `{ html, errors }`, log any errors, and surface the returned `html`. Save the returned HTML to `dist/spike-output.html` at the repo root (the file the Plan 05 client-render gate consumes) — provide a download/save affordance or a small helper that writes it. Open `dist/spike-output.html` in a browser and verify the hero renders with white text on the `#0B1624` background and the Calibri/Roboto font stack — entirely from inlined BLOCK_DEFAULTS, no mj-head injection.
  </action>
  <acceptance_criteria>
    - `app/client/src/App.tsx` contains a fetch POST to `/api/compile` sending `editor.getHtml()` output (grep `/api/compile`)
    - `dist/spike-output.html` exists and its content starts with `<!doctype html` (or `<!DOCTYPE html`)
    - Opening `dist/spike-output.html` in a browser shows white text on dark `#0B1624` background, Calibri/Roboto stack, no dark-on-dark text (manual observation — spike exception)
    - SUMMARY records any MJML compile warnings/errors returned for the assembled canvas
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full editor verification (EDIT-01..05) — drag/reorder/inline/undo</name>
  <read_first>
    - .planning/phases/01-feasibility-spike-editor-core/01-RESEARCH.md (lines 683-694 Criterion 2 & 3 procedure; lines 47-49 EDIT-02/03/04 support; lines 516-521 Pitfall 3)
    - .planning/phases/01-feasibility-spike-editor-core/01-VALIDATION.md (lines 38-44 Per-Task Verification Map; lines 63-66 manual verifications)
  </read_first>
  <action>
    Run the full editor capability verification with both branded blocks now available. Drop each of the 5 generic block types (mj-text, mj-image, mj-button, columns [1/2/3], mj-divider, mj-spacer) plus ddroidd-hero and ddroidd-projects onto the canvas (EDIT-01, EDIT-05). Reorder two blocks by drag and delete one (EDIT-02). Double-click an mj-text and edit it inline (EDIT-03). Apply an edit, undo it with Ctrl+Z, redo with Ctrl+Y (EDIT-04). Record each result in the SUMMARY mapped to its requirement ID. If any block fails to drop, check the `'grapesjs-mjml'` hardcoded key (issue #223) before anything else.
  </action>
  <acceptance_criteria>
    - All 5 generic block types + ddroidd-hero + ddroidd-projects successfully drop onto the canvas (manual observation — spike exception per VALIDATION.md framework=None)
    - Two blocks reordered by drag and one block deleted; canvas updates (EDIT-02)
    - An mj-text edited inline; content changes persist on the canvas (EDIT-03)
    - Ctrl+Z reverts a new edit and Ctrl+Y re-applies it (EDIT-04)
    - SUMMARY maps EDIT-01..05 each to a PASS/FAIL observation
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/compile | Same boundary as Plan 02; this plan is the first real caller of the endpoint from the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | POST /api/compile body | mitigate | Inherited from Plan 02 (`express.json({ limit: '1mb' })`). No new mitigation needed in this plan. |
| T-01-02 | Spoofing | CORS / Vite proxy | mitigate | Inherited from Plan 02 (`cors({ origin: 'http://localhost:5173' })`); browser calls via the Vite `/api` proxy. |

No new trust boundary introduced — the compile endpoint and its mitigations are owned by Plan 02 (`01-02`).
</threat_model>

<verification>
- `getHtml()` shape documented (open question #2 resolved).
- EDIT-01..05 all demonstrated with both branded blocks + 5 generic.
- End-to-end compile writes `dist/spike-output.html`; browser render confirms inlined defaults.
</verification>

<success_criteria>
- ROADMAP criterion 2 (drag/drop + reorder, ≥2 branded + 5 generic) verified.
- Editor → compile → file path works end-to-end.
- `dist/spike-output.html` ready for the Plan 05 client-render gate.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike-editor-core/01-03-SUMMARY.md` when done. Record: getHtml() shape, EDIT-01..05 PASS/FAIL map, compile warnings, and confirmation that `dist/spike-output.html` was written and renders in a browser.
</output>
