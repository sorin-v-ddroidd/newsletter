{
  "name": "ddroidd-newsletter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mjml src/pages/**.mjml -o dist/index.html",
    "watch": "mjml -w src/**/*.mjml -o dist",
    "build-pages": "mjml src/pages/**/**.mjml -o dist/",
    "watch-pages": "mjml -w src/pages/**/**.mjml -o dist",
    "dev": "concurrently \"nodemon --watch src --ext mjml --exec \\\"npm run build-pages\\\"\" \"browser-sync start --server dist --files 'dist/**/*' --port 8080 --no-notify\""
  },
  "keywords": [],
  "author": "DDROIDD team",
  "license": "ISC",
  "dependencies": {
    "mjml": "^4.15.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.3"
  }
}
